
/*    super lame, do something better later
fun int buttonPoller()
{
    while(true)
    {
        buttonEvent => now;
        if (buttonEvent.nextMsg() != 0)
        {
            buttonEvent.getInt() => buttonState;
            Button.broadcast();
            return buttonState;
            
        }
        else{                                 
        }
        */ 